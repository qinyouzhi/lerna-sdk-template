import { Box } from 'fbm-ui';
import RootProvider from '@components/RootProvider';

const {{ properCase name }}: React.FC = () => {
  return (
    <RootProvider>
      <Box>{{ properCase name }}</Box>
    </RootProvider>
  );
}

export default {{ properCase name }};
