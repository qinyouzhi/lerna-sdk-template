import { useEffect } from 'react';
import { {{ properCase name }} } from '../../../packages/{{ name }}/src';

const Example: React.FC = () => {
  useEffect(() => {}, []);

  return <{{ properCase name }} />;
};

export default Example;
